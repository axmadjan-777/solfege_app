name: solfege_app
description: Сольфеджио — тренировка слуха для музыкантов.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.9.1
  shared_preferences: ^2.5.3
  audioplayers: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  http: ^1.2.0

flutter:
  uses-material-design: true
